package io.github.donut.proj.listener;

/**
 * Who ever implements this can act as a server feeding info to subscribed Observers
 * @see IObserver
 * @author Kord Boniadi
 */
public interface ISubject {
/*
    void register(ISubject subj, IObserver obs);
    void unregister(ISubject subj, IObserver obs);
    void notify(ISubject subj, Object obj);
    void removeAllObserver(ISubject subj);
    boolean hasObservers(ISubject subj);
*/
}
